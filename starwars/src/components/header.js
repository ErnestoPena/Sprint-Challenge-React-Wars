import React from 'react';

const MyHeader = (props) => {
    return(
        <div>
           <h1 className="Header"><button className="before"> Previous </button>React Wars<button className="after">Next</button></h1>
        </div>
    )
}

export default MyHeader;